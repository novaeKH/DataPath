# Draft: Сравнение DT / RF / GB / CatBoost — когда что выбирать

- **Target content ID:** новый `concept.ml.ensemble-comparison` или дополнить `concept.ml.xgboost-lightgbm-and-catboost`
- **Target path:** новый файл `10 Знания/ML/01 Classical ML/Ensemble Comparison.md`
- **Проблема:** сравнение есть в уроке 10 (таблица), но нет канонической заметки для retrieval с decision framework.
- **Связанные skills:** `ml.tree_ensembles`, `ml.bias_variance_regularization`, `ml.error_analysis`
- **Ожидаемые semantic roles:** comparison, pitfalls
- **Нужно:** текст, таблица
- **Требует ручной проверки:** да — особенно выбор «дефолтной» библиотеки (зависит от экосистемы).

---

## Когда что выбирать: Decision Framework

### Быстрый выбор (decision tree выбора модели)

```
Сколько данных?
├── < 1 000 объектов
│   └── Decision Tree (интерпретируемость) или RF (качество)
├── 1 000 – 100 000 объектов
│   ├── Нужна интерпретируемость?
│   │   └── Decision Tree (визуализация, правила)
│   ├── Табличные данные, смешанные типы?
│   │   └── Random Forest (хороший baseline)
│   └── Нужно максимальное качество?
│       └── Gradient Boosting (XGBoost/LightGBM/CatBoost)
└── > 100 000 объектов
    ├── Много категориальных признаков?
    │   └── CatBoost (ordered encoding)
    ├── Важна скорость обучения?
    │   └── LightGBM (histograms, leaf-wise)
    └── Нужна тонкая настройка?
        └── XGBoost (зрелая экосистема, много параметров)
```

### Сравнительная таблица

| Критерий | Decision Tree | Random Forest | Gradient Boosting |
|---|---|---|---|
| **Интерпретируемость** | Отличная (видно правила) | Средняя (feature importance) | Низкая (чёрный ящик) |
| **Качество (tabular)** | Низкое–среднее | Хорошее | Отличное (SOTA) |
| **Скорость обучения** | Быстрая | Средняя (можно параллельно) | Медленная (последовательно) |
| **Скорость инференса** | Быстрая | Средняя ($M$ деревьев) | Средняя ($M$ деревьев) |
| **Чувствительность к шуму** | Высокая (overfit) | Низкая (averaging) | Средняя (early stopping) |
| **Пропуски в данных** | Поддерживает (surrogate splits) | Не нативно | Зависит от библиотеки |
| **Категориальные признаки** | Требуют кодирования | Требуют кодирования | CatBoost: нативно |
| **Out-of-bag оценка** | Нет | Да (честная валидация) | Нет (нужен validation set) |
| **GPU-обучение** | Нет | Нет | Да (XGBoost, LightGBM, CatBoost) |

### Типичные ошибки выбора

1. **Использовать глубокое дерево на малых данных** — переобучение, RF или GB с early stopping лучше.
2. **Настраивать XGBoost без валидации** — tuning по test-выборке даёт смещённую оценку качества.
3. **Игнорировать CatBoost для категорий** — ручное кодирование (OneHot, Label) часто хуже ordered target statistic.
4. **Выбирать boosting для задачи, где важна интерпретируемость** — банки, медицина часто требуют explainability → Decision Tree или RF с SHAP.
5. **Сравнивать модели без calibration вероятностей** — RF и GB дают разные шкалы «уверенности», требуется Platt scaling или isotonic regression.

### Ответ для собеседования

«Для табличных данных baseline — Random Forest: стабилен, не требует сложного тюнинга, даёт честную OOB-оценку. Если нужно максимальное качество — Gradient Boosting (XGBoost/LightGBM/CatBoost). CatBoost предпочтителен при большом количестве категориальных признаков — ordered encoding защищает от data leakage. LightGBM быстрее на больших данных за счёт histogram-based обучения. Decision Tree использую для интерпретируемости, визуализации правил и как building block для ансамблей. Важно: boosting-модели нельзя обучать параллельно — каждый шаг зависит от предыдущего.»

**Примечание для ручной проверки:**
- Сравнительная таблица частично дублирует таблицу в уроке 10 — решить, оставить одну каноническую или развести по целям (урок — для студента, concept — для retrieval).
- Decision framework можно оформить как Mermaid-диаграмму.
- Добавить информацию о SHAP для интерпретации ансамблей.
