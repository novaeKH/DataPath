"""Интерактивные лаборатории (Фаза 3).

Каждая лаборатория — Python-класс Lab: metadata, схема параметров и
детерминированный расчёт. Никакой ML-логики во frontend: клиент отправляет
параметры в POST /api/labs/{lab_id}/run и получает готовые результаты.
"""
