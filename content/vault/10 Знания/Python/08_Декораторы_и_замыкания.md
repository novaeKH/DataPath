---
title: "08. Декораторы и замыкания"
type: "concept"
area: "python"
status: "active"
source: "Python_Interview_Preparation"
integrated: "2026-07-31"
tags: ["python/core", "interview/python"]
id: concept.python.08-dekoratory-i-zamykaniia
schema_version: 2
language: ru
rag: include
rag_collection: knowledge
app: source
---
# 08. Декораторы и замыкания

[[07_Исключения_и_Context_Manager|← Предыдущий]] · [[09_Память_GC_GIL|Следующий →]]

## От вложенной функции к декоратору

Декоратор получает функцию и возвращает функцию-обёртку:

```python
from collections.abc import Callable
from functools import wraps
from typing import ParamSpec, TypeVar

P = ParamSpec("P")
R = TypeVar("R")

def log_call(function: Callable[P, R]) -> Callable[P, R]:
    @wraps(function)
    def wrapper(*args: P.args, **kwargs: P.kwargs) -> R:
        print(f"call {function.__name__}")
        return function(*args, **kwargs)
    return wrapper
```

```python
@log_call
def predict(rows: list[list[float]]) -> list[float]:
    return [0.0] * len(rows)
```

Синтаксис эквивалентен `predict = log_call(predict)`. `wraps` сохраняет имя, docstring и метаданные исходной функции.

## Замыкание

`wrapper` продолжает видеть `function` после завершения `log_call`. Это и есть замыкание: функция плюс доступ к enclosing-окружению.

## Измерение времени

```python
from time import perf_counter

def timed(function: Callable[P, R]) -> Callable[P, R]:
    @wraps(function)
    def wrapper(*args: P.args, **kwargs: P.kwargs) -> R:
        started = perf_counter()
        try:
            return function(*args, **kwargs)
        finally:
            print(f"{function.__name__}: {perf_counter() - started:.4f}s")
    return wrapper
```

Для честного benchmark используйте `timeit`/`pyperf`; этот декоратор — наблюдаемость, а не научное измерение.

## Декоратор с параметрами

```python
def repeat(times: int) -> Callable[[Callable[P, R]], Callable[P, R]]:
    if times < 1:
        raise ValueError("times must be positive")

    def decorator(function: Callable[P, R]) -> Callable[P, R]:
        @wraps(function)
        def wrapper(*args: P.args, **kwargs: P.kwargs) -> R:
            result: R
            for _ in range(times):
                result = function(*args, **kwargs)
            return result
        return wrapper
    return decorator
```

Три уровня вызова: `repeat(3)` фиксирует параметры; `decorator(function)` фиксирует функцию; `wrapper(...)` обрабатывает вызов.

## Когда применять

Логирование, метрики, retries, кэширование, авторизация. Не скрывайте существенную бизнес-логику и не меняйте неожиданно тип возвращаемого значения.

## Мини-контрольная

1. Что получает декоратор?
2. Зачем `wraps`?
3. Сколько функций вложено в декораторе с параметрами?
4. Где возникает closure?
5. Почему timing одного вызова ненадёжен?

<details><summary>Ответы</summary>
1. Функцию/объект декорирования. 2. Сохранить метаданные и `__wrapped__`. 3. Обычно три уровня. 4. Wrapper использует имя из внешней функции. 5. Шум, прогрев, системная нагрузка.
</details>
