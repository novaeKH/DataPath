---
title: "Two Pointers — задачи без решений"
type: "practice"
area: "algorithms"
status: "active"
source: "Python_Interview_Preparation"
integrated: "2026-07-31"
rag: "include"
tags: ["algorithms/practice", "practice/tasks"]
id: practice.algorithms.two-pointers-zadachi-bez-reshenii
schema_version: 2
language: ru
rag_collection: practice
app: source
---
# Two Pointers — задачи без решений

[[00_Каталог_задач|← Каталог]] · [[02_Two_Pointers_подсказки|Подсказки]]

Каждая карточка содержит уровень, полное условие, входные данные, ожидаемый результат, примеры, ограничения и сигнатуру.

## LC 344 — Reverse String

**Сложность:** Easy

**Условие:** Разверните `list[str]` на месте.

**Дано:** Изменяемый список символов `chars`.

**Нужно вернуть:** Ничего; развернуть исходный список на месте.

**Примеры:** `["h","i"] → ["i","h"]`; `[] → []`.

**Ограничения:** O(1) дополнительной памяти.

**Сигнатура:**

```python
def reverse_string(chars: list[str]) -> None:
    raise NotImplementedError
```

**Перед кодом:** что уже верно вне двух указателей?

**Моё решение:**

```python

```

## LC 125 — Valid Palindrome

**Сложность:** Easy

**Условие:** Игнорируя регистр и неалфавитно-цифровые символы, проверьте палиндром.

**Дано:** Строка `text` с буквами, цифрами и произвольными другими символами.

**Нужно вернуть:** `True`, если очищенная строка является палиндромом, иначе `False`.

**Примеры:** `"A man, a plan, a canal: Panama" → True`; `"race a car" → False`.

**Ограничения:** строка может быть пустой.

**Сигнатура:**

```python
def is_palindrome(text: str) -> bool:
    raise NotImplementedError
```

**Перед кодом:** когда каждый указатель можно сдвинуть?

**Моё решение:**

```python

```

## LC 26 — Remove Duplicates from Sorted Array

**Сложность:** Easy

**Условие:** Измените отсортированный массив так, чтобы первые `k` элементов были уникальны; верните `k`.

**Дано:** Отсортированный по неубыванию список `nums`.

**Нужно вернуть:** Число уникальных элементов `k`; первые `k` позиций массива также нужно изменить.

**Примеры:** `[1,1,2] → k=2`, префикс `[1,2]`.

**Ограничения:** in-place, хвост после k не важен.

**Сигнатура:**

```python
def remove_duplicates(nums: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** что означает `write`?

**Моё решение:**

```python

```

## LC 27 — Remove Element

**Сложность:** Easy

**Условие:** Удалите все `value` in-place и верните длину полезного префикса.

**Дано:** Список `nums` и удаляемое значение `value`.

**Нужно вернуть:** Длину `k` полезного префикса после удаления; первые `k` позиций изменить на месте.

**Примеры:** `[3,2,2,3], 3 → k=2`, префикс `[2,2]`.

**Ограничения:** порядок полезного префикса может быть любым по оригиналу; выберите ясный вариант.

**Сигнатура:**

```python
def remove_element(nums: list[int], value: int) -> int:
    raise NotImplementedError
```

**Моё решение:**

```python

```

## LC 88 — Merge Sorted Array

**Сложность:** Easy

**Условие:** Слейте `nums2` в буфер `nums1` на месте; в `nums1` первые m элементов полезны и есть n свободных позиций.

**Дано:** Буфер `nums1`, число полезных элементов `m`, отсортированный `nums2` и его длина `n`.

**Нужно вернуть:** Ничего; записать итоговое слияние непосредственно в `nums1`.

**Примеры:** `[1,2,3,0,0,0],3,[2,5,6],3 → [1,2,2,3,5,6]`.

**Ограничения:** оба полезных префикса отсортированы.

**Сигнатура:**

```python
def merge_sorted(nums1: list[int], m: int, nums2: list[int], n: int) -> None:
    raise NotImplementedError
```

**Перед кодом:** почему безопаснее писать с конца?

**Моё решение:**

```python

```

## LC 167 — Two Sum II

**Сложность:** Medium

**Условие:** В отсортированном массиве найдите два числа с суммой target; верните 1-based индексы.

**Дано:** Отсортированный список `numbers` и целевая сумма `target`.

**Нужно вернуть:** Два 1-based индекса элементов, дающих `target`.

**Примеры:** `[2,7,11,15],9 → [1,2]`.

**Ограничения:** ровно один ответ, нельзя один элемент дважды.

**Сигнатура:**

```python
def two_sum_sorted(numbers: list[int], target: int) -> list[int]:
    raise NotImplementedError
```

**Перед кодом:** какой указатель двигать при слишком маленькой сумме?

**Моё решение:**

```python

```

## LC 283 — Move Zeroes

**Сложность:** Easy

**Условие:** Переместите нули в конец, сохранив порядок ненулевых, in-place.

**Дано:** Список целых чисел `nums`.

**Нужно вернуть:** Ничего; переместить нули в конец на месте, сохранив порядок остальных элементов.

**Примеры:** `[0,1,0,3,12] → [1,3,12,0,0]`.

**Ограничения:** не создавайте копию массива.

**Сигнатура:**

```python
def move_zeroes(nums: list[int]) -> None:
    raise NotImplementedError
```

**Моё решение:**

```python

```

## LC 11 — Container With Most Water

**Сложность:** Medium

**Условие:** Выберите две вертикальные линии, максимизирующие площадь.

**Дано:** Список неотрицательных высот `height`.

**Нужно вернуть:** Максимальную площадь контейнера, образованного двумя линиями.

**Примеры:** `[1,8,6,2,5,4,8,3,7] → 49`.

**Ограничения:** минимум две высоты, значения неотрицательны.

**Сигнатура:**

```python
def max_area(height: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** почему безопасно двигать более короткую линию?

**Моё решение:**

```python

```
