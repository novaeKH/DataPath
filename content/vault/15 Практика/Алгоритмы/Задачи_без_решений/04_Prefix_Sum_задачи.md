---
title: "Prefix Sum — задачи без решений"
type: "practice"
area: "algorithms"
status: "active"
source: "Python_Interview_Preparation"
integrated: "2026-07-31"
rag: "include"
tags: ["algorithms/practice", "practice/tasks"]
id: practice.algorithms.prefix-sum-zadachi-bez-reshenii
schema_version: 2
language: ru
rag_collection: practice
app: source
---
# Prefix Sum — задачи без решений

[[00_Каталог_задач|← Каталог]] · [[04_Prefix_Sum_подсказки|Подсказки]]

Каждая карточка содержит уровень, полное условие, входные данные, ожидаемый результат, примеры, ограничения и сигнатуру.

## LC 1480 — Running Sum of 1D Array

**Сложность:** Easy

**Условие:** Верните накопительные суммы.

**Дано:** Список целых чисел `nums`.

**Нужно вернуть:** Список той же длины, где позиция `i` содержит сумму элементов `0..i`.

**Примеры:** `[1,2,3,4] → [1,3,6,10]`; `[] → []` локально.

**Ограничения:** Элементы — целые числа; в локальной версии пустой список допустим.

**Сигнатура:**

```python
def running_sum(nums: list[int]) -> list[int]:
    raise NotImplementedError
```

**Перед кодом:** что хранить вместо повторного `sum(nums[:i])`?

**Моё решение:**

```python

```

## LC 724 — Find Pivot Index

**Сложность:** Easy

**Условие:** Верните первый индекс, где сумма слева равна сумме справа, иначе `-1`.

**Дано:** Список целых чисел `nums`.

**Нужно вернуть:** Первый индекс с равными суммами слева и справа или `-1`.

**Примеры:** `[1,7,3,6,5,6] → 3`.

**Ограничения:** отрицательные числа возможны.

**Сигнатура:**

```python
def pivot_index(nums: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** как выразить right sum через total?

**Моё решение:**

```python

```

## LC 303 — Range Sum Query - Immutable

**Сложность:** Easy

**Условие:** Реализуйте класс `NumArray`, отвечающий на много запросов суммы от left до right включительно.

**Дано:** Неизменяемый список `nums`, затем множество запросов с границами `left` и `right`.

**Нужно вернуть:** Для каждого запроса сумму элементов от `left` до `right` включительно.

**Примеры:** `[-2,0,3,-5,2,-1]`, `sumRange(0,2) → 1`.

**Ограничения:** массив не меняется после создания.

**Сигнатура:**

```python
class NumArray:
    def __init__(self, nums: list[int]) -> None:
        raise NotImplementedError

    def sum_range(self, left: int, right: int) -> int:
        raise NotImplementedError
```

**Моё решение:**

```python

```

## LC 560 — Subarray Sum Equals K

**Сложность:** Medium

**Условие:** Посчитайте непрерывные подмассивы с суммой k.

**Дано:** Список целых чисел `nums` и целевая сумма `k`.

**Нужно вернуть:** Количество непрерывных подмассивов, сумма которых равна `k`.

**Примеры:** `[1,1,1],2 → 2`; `[1,-1,0],0 → 3`.

**Ограничения:** отрицательные числа и нули возможны.

**Сигнатура:**

```python
def subarray_sum(nums: list[int], k: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** если текущий prefix = p, какой прошлый prefix нужен?

**Моё решение:**

```python

```

## LC 238 — Product of Array Except Self

**Сложность:** Medium

**Условие:** Для каждого индекса верните произведение остальных элементов без деления.

**Дано:** Список целых чисел `nums`.

**Нужно вернуть:** Список, где каждый элемент равен произведению всех остальных элементов исходного списка.

**Примеры:** `[1,2,3,4] → [24,12,8,6]`.

**Ограничения:** O(n), деление запрещено; нули возможны.

**Сигнатура:**

```python
def product_except_self(nums: list[int]) -> list[int]:
    raise NotImplementedError
```

**Перед кодом:** какие два направления произведений нужны?

**Моё решение:**

```python

```
