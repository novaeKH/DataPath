---
title: "Greedy и DP — задачи без решений"
type: "practice"
area: "algorithms"
status: "active"
source: "Python_Interview_Preparation"
integrated: "2026-07-31"
rag: "include"
tags: ["algorithms/practice", "practice/tasks"]
id: practice.algorithms.greedy-i-dp-zadachi-bez-reshenii
schema_version: 2
language: ru
rag_collection: practice
app: source
---
# Greedy и DP — задачи без решений

[[00_Каталог_задач|← Каталог]] · [[11_Greedy_DP_подсказки|Подсказки]]

Каждая карточка содержит уровень, полное условие, входные данные, ожидаемый результат, примеры, ограничения и сигнатуру.

## LC 121 — Best Time to Buy and Sell Stock

**Сложность:** Easy

**Условие:** Одна покупка раньше одной продажи; верните максимальную прибыль или 0.

**Дано:** Список цен `prices`, где индекс соответствует дню.

**Нужно вернуть:** Максимальную прибыль от одной покупки и одной более поздней продажи или `0`.

**Примеры:** `[7,1,5,3,6,4] → 5`; убывающие цены → 0.

**Ограничения:** Разрешена не более чем одна транзакция; продать можно только после покупки.

**Сигнатура:**

```python
def max_profit(prices: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** какая лучшая информация о префиксе нужна?

**Моё решение:**

```python

```

## LC 53 — Maximum Subarray

**Сложность:** Medium

**Условие:** Верните максимальную сумму непустого непрерывного подмассива.

**Дано:** Непустой список целых чисел `nums`.

**Нужно вернуть:** Максимальную сумму непустого непрерывного подмассива.

**Примеры:** `[-2,1,-3,4,-1,2,1,-5,4] → 6`.

**Ограничения:** Подмассив должен быть непустым; исходный список также непустой.

**Сигнатура:**

```python
def max_subarray(nums: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** что значит «лучший подмассив, заканчивающийся здесь»?

**Моё решение:**

```python

```

## LC 70 — Climbing Stairs

**Сложность:** Easy

**Условие:** Можно делать 1 или 2 шага; сколько способов достичь n?

**Дано:** Положительное число ступеней `n`; за ход можно пройти 1 или 2 ступени.

**Нужно вернуть:** Количество различных способов попасть ровно на ступень `n`.

**Примеры:** `2 → 2`; `3 → 3`.

**Ограничения:** n положителен в оригинале.

**Сигнатура:**

```python
def climb_stairs(n: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** состояние, база, переход, порядок?

**Моё решение:**

```python

```

## LC 198 — House Robber

**Сложность:** Medium

**Условие:** Максимизируйте сумму, не выбирая соседние дома.

**Дано:** Список неотрицательных сумм денег `nums` в домах вдоль одной улицы.

**Нужно вернуть:** Максимальную сумму без выбора двух соседних домов.

**Примеры:** `[2,7,9,3,1] → 12`.

**Ограничения:** Суммы неотрицательны; два соседних дома выбирать нельзя.

**Сигнатура:**

```python
def rob(nums: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** выбор «взять» зависит от какого прошлого состояния?

**Моё решение:**

```python

```

## LC 322 — Coin Change

**Сложность:** Medium

**Условие:** минимальное число монет для amount, неограниченное число каждой; `-1`, если невозможно.

**Дано:** Положительные номиналы `coins` и неотрицательная сумма `amount`.

**Нужно вернуть:** Минимальное число монет для набора `amount` или `-1`, если это невозможно.

**Примеры:** coins `[1,2,5]`, amount 11 → 3; `[2]`, 3 → -1.

**Ограничения:** положительные номиналы, amount неотрицателен.

**Сигнатура:**

```python
def coin_change(coins: list[int], amount: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** что означает `dp[a]`, чем инициализировать невозможное?

**Моё решение:**

```python

```
