---
title: "Trees, BFS, DFS и Grid — задачи без решений"
type: "practice"
area: "algorithms"
status: "active"
source: "Python_Interview_Preparation"
integrated: "2026-07-31"
rag: "include"
tags: ["algorithms/practice", "practice/tasks"]
id: practice.algorithms.trees-bfs-dfs-i-grid-zadachi-bez-reshenii
schema_version: 2
language: ru
rag_collection: practice
app: source
---
# Trees, BFS, DFS и Grid — задачи без решений

[[00_Каталог_задач|← Каталог]] · [[10_Trees_и_Grid_подсказки|Подсказки]]

```python
class TreeNode:
    def __init__(self, val: int, left: "TreeNode | None" = None, right: "TreeNode | None" = None) -> None:
        self.val, self.left, self.right = val, left, right
```

Каждая карточка содержит уровень, полное условие, входные данные, ожидаемый результат, примеры, ограничения и сигнатуру.

## LC 104 — Maximum Depth of Binary Tree

**Сложность:** Easy

**Условие:** Верните максимальную глубину; пустое дерево имеет глубину 0.

**Дано:** Корень бинарного дерева `root` или `None`.

**Нужно вернуть:** Максимальную глубину дерева; для пустого дерева — `0`.

**Примеры:** `[3,9,20,null,null,15,7] → 3`; пустое дерево `[] → 0`.

**Ограничения:** Дерево может быть пустым; глубина считается как число узлов на пути от корня до листа.

**Сигнатура:**

```python
def max_depth(root: TreeNode | None) -> int:
    raise NotImplementedError
```

**Моё решение:**

```python

```

## LC 100 — Same Tree

**Сложность:** Easy

**Условие:** Проверьте, одинаковы ли структура и значения двух деревьев.

**Дано:** Корни двух бинарных деревьев `p` и `q`.

**Нужно вернуть:** `True`, если деревья совпадают по структуре и значениям.

**Примеры:** `[1,2,3]` и `[1,2,3] → True`; `[1,2]` и `[1,null,2] → False`.

**Ограничения:** Оба дерева могут быть пустыми; должны совпасть и структура, и значения соответствующих узлов.

**Сигнатура:**

```python
def is_same_tree(p: TreeNode | None, q: TreeNode | None) -> bool:
    raise NotImplementedError
```

**Перед кодом:** какие три базовых случая для пары узлов?

**Моё решение:**

```python

```

## LC 226 — Invert Binary Tree

**Сложность:** Easy

**Условие:** Поменяйте левое и правое поддерево каждого узла.

**Дано:** Корень бинарного дерева `root` или `None`.

**Нужно вернуть:** Корень дерева, в котором у каждого узла поменяны местами левое и правое поддеревья.

**Примеры:** `[4,2,7,1,3,6,9] → [4,7,2,9,6,3,1]`; `[] → []`.

**Ограничения:** Дерево может быть пустым; требуется изменить связи каждого существующего узла.

**Сигнатура:**

```python
def invert_tree(root: TreeNode | None) -> TreeNode | None:
    raise NotImplementedError
```

**Моё решение:**

```python

```

## LC 102 — Binary Tree Level Order Traversal

**Сложность:** Medium

**Условие:** Верните значения по уровням.

**Дано:** Корень бинарного дерева `root` или `None`.

**Нужно вернуть:** Вложенный список значений дерева по уровням сверху вниз.

**Примеры:** `[3,9,20,null,null,15,7] → [[3],[9,20],[15,7]]`.

**Ограничения:** Для пустого дерева вернуть пустой список; порядок внутри уровня — слева направо.

**Сигнатура:**

```python
def level_order(root: TreeNode | None) -> list[list[int]]:
    raise NotImplementedError
```

**Перед кодом:** зачем фиксировать размер queue до уровня?

**Моё решение:**

```python

```

## LC 200 — Number of Islands

**Сложность:** Medium

**Условие:** В grid из `"1"` и `"0"` посчитайте компоненты земли по четырём направлениям.

**Дано:** Прямоугольная grid из строковых значений `"1"` и `"0"`.

**Нужно вернуть:** Количество компонент суши, соединённых по четырём направлениям.

**Примеры:** `[["1","1","0"],["1","0","0"],["0","0","1"]] → 2`.

**Ограничения:** Grid может быть пустой; суша соединяется только вверх, вниз, влево и вправо.

**Сигнатура:**

```python
def num_islands(grid: list[list[str]]) -> int:
    raise NotImplementedError
```

**Перед кодом:** когда увеличивать count и когда запускать обход?

**Моё решение:**

```python

```

## LC 733 — Flood Fill

**Сложность:** Easy

**Условие:** Перекрасьте связную область исходного цвета.

**Дано:** Матрица цветов `image`, координаты `sr`, `sc` и новый цвет `color`.

**Нужно вернуть:** Матрицу после перекраски всей связной области исходного цвета.

**Примеры:** `[[1,1,1],[1,1,0],[1,0,1]], sr=1,sc=1,color=2`.

**Ограничения:** Матрица прямоугольная, стартовые координаты допустимы; соседство только по четырём направлениям.

**Сигнатура:**

```python
def flood_fill(image: list[list[int]], sr: int, sc: int, color: int) -> list[list[int]]:
    raise NotImplementedError
```

**Перед кодом:** что если новый цвет равен исходному?

**Моё решение:**

```python

```

## LC 994 — Rotting Oranges

**Сложность:** Medium

**Условие:** За минуту rotten заражает свежих соседей; верните минимальное время или `-1`.

**Дано:** Grid: `0` — пусто, `1` — свежий апельсин, `2` — испорченный.

**Нужно вернуть:** Минимальное число минут до порчи всех свежих апельсинов или `-1`.

**Примеры:** `[[2,1,1],[1,1,0],[0,1,1]] → 4`; `[[0,2]] → 0`.

**Ограничения:** 0 пусто, 1 fresh, 2 rotten; четыре направления.

**Сигнатура:**

```python
def oranges_rotting(grid: list[list[int]]) -> int:
    raise NotImplementedError
```

**Перед кодом:** почему это multi-source BFS?

**Моё решение:**

```python

```
