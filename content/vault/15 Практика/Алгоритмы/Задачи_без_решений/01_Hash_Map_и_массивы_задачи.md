---
title: "Hash Map и массивы — задачи без решений"
type: "practice"
area: "algorithms"
status: "active"
source: "Python_Interview_Preparation"
integrated: "2026-07-31"
rag: "include"
tags: ["algorithms/practice", "practice/tasks"]
id: practice.algorithms.hash-map-i-massivy-zadachi-bez-reshenii
schema_version: 2
language: ru
rag_collection: practice
app: source
---
# Hash Map и массивы — задачи без решений

[[00_Каталог_задач|← Каталог]] · [[01_Hash_Map_и_массивы_подсказки|Подсказки]]

Для каждой задачи ответьте на [[00_Каталог_задач#Общие 15 вопросов перед кодом|15 вопросов]]. Код ниже намеренно пуст.

Каждая карточка содержит уровень, полное условие, входные данные, ожидаемый результат, примеры, ограничения и сигнатуру.

## LC 1 — Two Sum

**Сложность:** Easy

**Условие:** Верните индексы двух разных элементов, сумма которых равна `target`.

**Дано:** Список целых чисел `nums` и целое число `target`.

**Нужно вернуть:** Пару индексов двух разных элементов, сумма которых равна `target`.

**Примеры:** `[2,7,11,15], 9 → (0,1)`; `[3,3], 6 → (0,1)`.

**Ограничения:** ровно один ответ; нельзя использовать элемент дважды.

**Сигнатура:**

```python
def two_sum(nums: list[int], target: int) -> tuple[int, int]:
    raise NotImplementedError

assert two_sum([2, 7, 11, 15], 9) == (0, 1)
```

**Перед кодом:** что повторяет brute force? Что нужно запомнить о префиксе?

**Моё решение:**

```python

```

## LC 217 — Contains Duplicate

**Сложность:** Easy

**Условие:** Верните `True`, если число встречается хотя бы дважды.

**Дано:** Список целых чисел `nums`.

**Нужно вернуть:** `True`, если хотя бы одно значение встречается повторно, иначе `False`.

**Примеры:** `[1,2,3,1] → True`; `[1,2,3] → False`.

**Ограничения:** пустой список допустим в локальной версии.

**Сигнатура:**

```python
def contains_duplicate(nums: list[int]) -> bool:
    raise NotImplementedError

assert contains_duplicate([1, 2, 1])
assert not contains_duplicate([])
```

**Перед кодом:** какая структура отвечает на «уже видел»?

**Моё решение:**

```python

```

## LC 242 — Valid Anagram

**Сложность:** Easy

**Условие:** Проверьте, можно ли переставить символы `s`, чтобы получить `t`.

**Дано:** Две строки `s` и `t`.

**Нужно вернуть:** `True`, если строки являются анаграммами, иначе `False`.

**Примеры:** `"anagram","nagaram" → True`; `"rat","car" → False`.

**Ограничения:** учитывайте точные символы; длины могут различаться.

**Сигнатура:**

```python
def is_anagram(s: str, t: str) -> bool:
    raise NotImplementedError
```

**Перед кодом:** что сравнить после подсчёта?

**Моё решение:**

```python

```

## LC 49 — Group Anagrams

**Сложность:** Medium

**Условие:** Сгруппируйте строки, являющиеся анаграммами.

**Дано:** Список строк `words`.

**Нужно вернуть:** Группы строк, где каждая группа состоит из взаимных анаграмм.

**Примеры:** `["eat","tea","tan","ate"] → [["eat","tea","ate"],["tan"]]` с любым порядком групп.

**Ограничения:** строки из строчных английских букв в оригинале.

**Сигнатура:**

```python
def group_anagrams(words: list[str]) -> list[list[str]]:
    raise NotImplementedError
```

**Перед кодом:** каким hashable-ключом описать группу?

**Моё решение:**

```python

```

## LC 349 — Intersection of Two Arrays

**Сложность:** Easy

**Условие:** Верните уникальные общие значения двух массивов в любом порядке.

**Дано:** Два списка целых чисел `nums1` и `nums2`.

**Нужно вернуть:** Уникальные значения, присутствующие в обоих списках, в любом порядке.

**Примеры:** `[1,2,2,1], [2,2] → [2]`.

**Ограничения:** порядок ответа не важен.

**Сигнатура:**

```python
def intersection(nums1: list[int], nums2: list[int]) -> list[int]:
    raise NotImplementedError
```

**Перед кодом:** почему multiset не нужен?

**Моё решение:**

```python

```

## LC 350 — Intersection of Two Arrays II

**Сложность:** Easy

**Условие:** Верните пересечение с учётом кратности.

**Дано:** Два списка целых чисел `nums1` и `nums2`.

**Нужно вернуть:** Пересечение с учётом минимальной кратности каждого значения.

**Примеры:** `[1,2,2,1], [2,2] → [2,2]`.

**Ограничения:** порядок ответа не важен.

**Сигнатура:**

```python
def intersect_multiset(nums1: list[int], nums2: list[int]) -> list[int]:
    raise NotImplementedError
```

**Перед кодом:** чью частоту уменьшать?

**Моё решение:**

```python

```

## LC 387 — First Unique Character in a String

**Сложность:** Easy

**Условие:** Верните индекс первого символа с частотой 1, иначе `-1`.

**Дано:** Строка `text`.

**Нужно вернуть:** Индекс первого символа с частотой 1 или `-1`, если такого символа нет.

**Примеры:** `"leetcode" → 0`; `"aabb" → -1`.

**Ограничения:** строка может быть пустой в локальной версии.

**Сигнатура:**

```python
def first_unique_char(text: str) -> int:
    raise NotImplementedError
```

**Перед кодом:** почему полезны два последовательных прохода?

**Моё решение:**

```python

```

## LC 169 — Majority Element

**Сложность:** Easy

**Условие:** Верните элемент, встречающийся чаще `n/2`; он гарантирован.

**Дано:** Непустой список целых чисел `nums`, в котором majority-элемент существует.

**Нужно вернуть:** Значение, встречающееся чаще `len(nums) / 2`.

**Примеры:** `[3,2,3] → 3`; `[2,2,1,1,1,2,2] → 2`.

**Ограничения:** список непустой; majority существует.

**Сигнатура:**

```python
def majority_element(nums: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** сначала решите через counts; затем изучите Boyer–Moore.

**Моё решение:**

```python

```

## LC 128 — Longest Consecutive Sequence

**Сложность:** Medium

**Условие:** Найдите длину самой длинной последовательности соседних целых значений независимо от порядка массива.

**Дано:** Неупорядоченный список целых чисел `nums`, возможно с дубликатами.

**Нужно вернуть:** Длину самой длинной цепочки последовательных целых значений.

**Примеры:** `[100,4,200,1,3,2] → 4`.

**Ограничения:** требуется ожидаемое O(n); дубликаты возможны.

**Сигнатура:**

```python
def longest_consecutive(nums: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** как узнать, что значение — начало цепочки?

**Моё решение:**

```python

```
