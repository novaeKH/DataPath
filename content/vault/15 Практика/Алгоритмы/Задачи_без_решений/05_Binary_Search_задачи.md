---
title: "Binary Search — задачи без решений"
type: "practice"
area: "algorithms"
status: "active"
source: "Python_Interview_Preparation"
integrated: "2026-07-31"
rag: "include"
tags: ["algorithms/practice", "practice/tasks"]
id: practice.algorithms.binary-search-zadachi-bez-reshenii
schema_version: 2
language: ru
rag_collection: practice
app: source
---
# Binary Search — задачи без решений

[[00_Каталог_задач|← Каталог]] · [[05_Binary_Search_подсказки|Подсказки]]

Каждая карточка содержит уровень, полное условие, входные данные, ожидаемый результат, примеры, ограничения и сигнатуру.

## LC 704 — Binary Search

**Сложность:** Easy

**Условие:** В отсортированном массиве найдите target, иначе `-1`.

**Дано:** Отсортированный список уникальных чисел `nums` и значение `target`.

**Нужно вернуть:** Индекс `target` или `-1`, если значение отсутствует.

**Примеры:** `[-1,0,3,5,9,12],9 → 4`; target 2 → -1.

**Ограничения:** значения уникальны в оригинале.

**Сигнатура:**

```python
def binary_search(nums: list[int], target: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** какой контракт диапазона используете?

**Моё решение:**

```python

```

## LC 35 — Search Insert Position

**Сложность:** Easy

**Условие:** Верните индекс target или позицию его вставки с сохранением сортировки.

**Дано:** Отсортированный список `nums` и значение `target`.

**Нужно вернуть:** Индекс `target` либо позицию, куда его нужно вставить без нарушения сортировки.

**Примеры:** `[1,3,5,6],5 → 2`; target 7 → 4.

**Ограничения:** `nums` отсортирован по неубыванию; позиция вставки может быть равна `len(nums)`.

**Сигнатура:**

```python
def search_insert(nums: list[int], target: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** ищете exact match или первую позицию `>= target`?

**Моё решение:**

```python

```

## LC 69 — Sqrt(x)

**Сложность:** Easy

**Условие:** Верните целую часть неотрицательного квадратного корня.

**Дано:** Неотрицательное целое число `x`.

**Нужно вернуть:** Целую часть квадратного корня из `x`.

**Примеры:** `8 → 2`; `0 → 0`.

**Ограничения:** x неотрицателен.

**Сигнатура:**

```python
def integer_sqrt(x: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** какой предикат меняется монотонно?

**Моё решение:**

```python

```

## LC 278 — First Bad Version

**Сложность:** Easy

**Условие:** Через API `isBadVersion(version)` найдите первую плохую версию.

**Дано:** Число версий `n` и доступ к предикату `isBadVersion(version)`.

**Нужно вернуть:** Номер первой плохой версии.

**Примеры:** n=5, first_bad=4 → 4.

**Ограничения:** версии 1..n, плохая версия и все после неё плохие.

**Сигнатура:**

```python
def first_bad_version(n: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** как не потерять возможный answer?

**Моё решение:**

```python

```

## LC 153 — Find Minimum in Rotated Sorted Array

**Сложность:** Medium

**Условие:** Найдите минимум в отсортированном массиве уникальных значений после поворота.

**Дано:** Непустой повёрнутый отсортированный список уникальных чисел `nums`.

**Нужно вернуть:** Минимальное значение массива.

**Примеры:** `[3,4,5,1,2] → 1`.

**Ограничения:** непустой, уникальные элементы.

**Сигнатура:**

```python
def find_min_rotated(nums: list[int]) -> int:
    raise NotImplementedError
```

**Перед кодом:** с какой границей сравнивать middle?

**Моё решение:**

```python

```

## LC 33 — Search in Rotated Sorted Array

**Сложность:** Medium

**Условие:** Найдите target в повёрнутом отсортированном массиве уникальных значений.

**Дано:** Повёрнутый отсортированный список уникальных чисел `nums` и `target`.

**Нужно вернуть:** Индекс `target` или `-1`, если значение отсутствует.

**Примеры:** `[4,5,6,7,0,1,2],0 → 4`; target 3 → -1.

**Ограничения:** Массив непустой, значения уникальны; до поворота он был отсортирован по возрастанию.

**Сигнатура:**

```python
def search_rotated(nums: list[int], target: int) -> int:
    raise NotImplementedError
```

**Перед кодом:** как определить отсортированную половину?

**Моё решение:**

```python

```
